$(document).ready(function () {   $( "#borrar" ).prop( "disabled", true );   var PeriodoLectivo = $("#PeriodoLectivo").kendoDropDownList({       dataTextField: "descripcion",       dataValueField: "id",       filter: "contains",       change: seleccionado,       optionLabel: "Nuevo",       suggest: true,       dataSource: {          transport: {             read: {                url: "../../scripts/php/get/getPeriodoLectivo.php",                dataType: "json",             }          }       }   });});function seleccionado(){   if ($("#PeriodoLectivo").val()>0){      $("#borrar").prop( "disabled", false );      document.getElementById("guardar").value="ACTUALIZAR";      showData();   }else{      $("#borrar").prop( "disabled", true );      document.getElementById("guardar").value="GUARDAR";      document.getElementById("descripcion").value="";      document.getElementById("fechaInicio").value="";      document.getElementById("fechaFin").value="";   }}function showData(request, response){   var xmlhttp = new XMLHttpRequest();   xmlhttp.onreadystatechange = function() {      if (xmlhttp.readyState == 4 && xmlhttp.status == 200) {         var result = JSON.parse(xmlhttp.responseText);         $.each(result, function (key, value) {            document.getElementById("descripcion").value=value.descripcion;            document.getElementById("fechaInicio").value=value.fechaInicio;            document.getElementById("fechaFin").value=value.fechaFin;         });      }   };   xmlhttp.open("GET", "../../scripts/php/get/getPeriodoLectivo.php?periodolectivo="+$("#PeriodoLectivo").val(), true);   xmlhttp.send();};
